{
  "routes": [
    {
      "method": "GET",
      "path": "/artist",
      "handler": "artist.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/artist",
      "handler": "artist.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/artist",
      "handler": "artist.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
