{
  "routes": [
    {
      "method": "GET",
      "path": "/song",
      "handler": "song.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/song",
      "handler": "song.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/song",
      "handler": "song.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
